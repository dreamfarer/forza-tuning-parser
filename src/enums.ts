/**The rounding mode options.*/
export enum RoundingMode {
  Whole = 'WHOLE',
  DoubleDigit = 'DOUBLE_DIGIT',
  SingleDigit = 'SINGLE_DIGIT',
  Half = 'HALF',
}

/** Unit system options. */
export enum UnitSystem {
  Imperial,
  Metric,
}

/** The position in relation to the car. */
export enum Position {
  Front,
  Rear,
}
